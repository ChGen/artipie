name: Artipie perftest part 2
on:
  workflow_run:
    workflows: [ perftest ]
    types:
      - completed
jobs:
  perf-test2:
    #if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v3
        name: 1st step of stage2
      - name: 2nd step of stage2
        run: env; ls -la; pwd

